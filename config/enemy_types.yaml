---
enemy_types:
  - republic
  - separatist
  - aerial
  - ground
  - player

enemy_types_applied_to_players:
  - player

allowed_player_attacks:
  if:
    aerial:
      if: {}
      else: Unattackable

    player:
      if: {}
      else: Unattackable

    republic:
      if: {}
      else: Unattackable

    separatist:
      if: {}
      else: Attackable

    ground:
      if: {}
      else: Attackable

  else: Unattackable
